# Vector store type configuration
VECTOR_STORE_TYPE=chroma
VECTOR_STORE_CONFIG_PATH=./config/vector/test_config.yaml

# Common vector settings
VECTOR_SIZE=1024
VECTOR_DISTANCE=cosine

# ChromaDB settings
CHROMA_CLIENT=local
CHROMA_PERSIST_DIR=./test_chroma_data
CHROMA_COLLECTION=test_collection
CHROMA_HOST=localhost
CHROMA_PORT=8000

# Qdrant settings (for future use)
QDRANT_HOST=localhost
QDRANT_PORT=6333
QDRANT_COLLECTION=test_collection
QDRANT_GRPC_PORT=6334

# Milvus settings (for future use)
MILVUS_HOST=localhost
MILVUS_PORT=19530
MILVUS_COLLECTION=test_collection

# Test specific settings
TEST_BATCH_SIZE=32
TEST_CLEANUP_ENABLED=true
TEST_PARALLEL_CONNECTIONS=4

# isA_MCP 项目结构说明

## 📁 目录结构概览

```
isA_MCP/
├── 📁 core/                    # 核心功能模块
├── 📁 tools/                   # 工具集合 (MCP工具)
├── 📁 tests/                   # 测试文件 (已整理)
├── 📁 scripts/                 # 脚本文件 (已整理)
├── 📁 docs/                    # 文档目录
├── 📁 data_storage/            # 数据存储
├── 📁 demo_data/              # 演示数据
├── 📁 deployment/             # 部署配置
├── 📁 static/                 # 静态文件
├── 📁 resources/              # 资源文件
├── 📁 config/                 # 配置文件
├── 📁 logs/                   # 日志文件
├── 📁 models/                 # 模型文件
├── 📁 prompts/                # 提示词模板
├── 📁 HowTos/                 # 使用指南
└── 📁 quality_monitoring/     # 质量监控
```

## 🧹 整理说明

### 已移动的文件

#### 测试文件 → `tests/`
- `test_*.py` → `tests/`
- 所有测试相关文件已统一管理

#### 调试文件 → `scripts/debug/`
- `debug_isa_image.py` → `scripts/debug/`
- `parse_xml_excel.py` → `scripts/`

#### 分析报告 → `docs/analysis_reports/`
- `error_fixes_summary.md` → `docs/analysis_reports/`
- `final_test_results.txt` → `docs/analysis_reports/`
- `prediction_test_analysis.md` → `docs/analysis_reports/`
- `test_results_fixed.txt` → `docs/analysis_reports/`

#### 临时数据 → `data_storage/`
- `temp_*.csv` → `data_storage/`

#### 架构文档 → `docs/architecture/`
- `ARCHITECTURE_COMPARISON.md` → `docs/architecture/`

#### 脚本文件 → `scripts/`
- `temp/bin/*` → `scripts/`
- 测试输出文件 → `static/test_output/`

#### 缓存和会话文件
- `temp/cache/vision/*` → `cache/vision/`
- `temp/sessions/*` → `logs/sessions/`
- `temp/models/*` → `models/`

### 已删除的文件/目录
- `temp/` 目录 (已清空并删除)
- `block` 文件 (无用的临时文件)

## 📋 文件分类规则

### 测试文件
- 所有 `test_*.py` 文件放在 `tests/` 目录
- 按功能分类到子目录 (如 `tests/debug/`, `tests/analysis/`)

### 脚本文件
- 所有脚本文件放在 `scripts/` 目录
- 按用途分类到子目录

### 文档文件
- 所有文档放在 `docs/` 目录
- 按类型分类到子目录

### 数据文件
- 临时数据放在 `data_storage/`
- 演示数据放在 `demo_data/`

### 日志文件
- 所有日志文件放在 `logs/` 目录
- 会话文件放在 `logs/sessions/`

## 🔧 .gitignore 更新

已更新 `.gitignore` 文件，添加了以下忽略规则：
- `tests/test_*.py` - 测试文件
- `data_storage/temp_*.csv` - 临时数据文件
- `data_storage/data_*.csv` - 数据文件

## ✅ 整理效果

1. **根目录更清洁**: 移除了所有杂乱的临时文件和测试文件
2. **结构更清晰**: 文件按功能分类到对应目录
3. **维护更容易**: 相关文件集中管理，便于维护
4. **Git更干净**: 更新了.gitignore，避免提交临时文件

## 🚀 后续建议

1. 定期清理临时文件
2. 保持目录结构的整洁
3. 新增文件时遵循分类规则
4. 定期检查.gitignore是否需要更新

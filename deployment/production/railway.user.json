{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "tools/services/user_service/Dockerfile.production"
  },
  "deploy": {
    "startCommand": "python tools/services/user_service/start_server.py --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3,
    "numReplicas": 1
  },
  "environments": {
    "production": {
      "variables": {
        "NODE_ENV": "production",
        "PYTHONPATH": ".",
        "PYTHONUNBUFFERED": "1",
        "SERVICE_NAME": "isa_service_user"
      },
      "serviceConnect": true
    }
  }
}
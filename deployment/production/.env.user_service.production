# User Service Production Environment Configuration

# User Service Specific Configuration
USER_SERVICE_PORT=8100
USER_SERVICE_HOST=0.0.0.0
USER_SERVICE_DEBUG=false
USER_SERVICE_RELOAD=false

# Auth0 Configuration (Production Environment)
AUTH0_DOMAIN=${AUTH0_DOMAIN_PRODUCTION}
AUTH0_AUDIENCE=${AUTH0_AUDIENCE_PRODUCTION}
AUTH0_CLIENT_ID=${AUTH0_CLIENT_ID_PRODUCTION}
AUTH0_CLIENT_SECRET=${AUTH0_CLIENT_SECRET_PRODUCTION}

# Stripe Configuration (Production Environment - Live Keys)
STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY_PRODUCTION}
STRIPE_WEBHOOK_SECRET=${STRIPE_WEBHOOK_SECRET_PRODUCTION}
STRIPE_PRO_PRICE_ID=${STRIPE_PRO_PRICE_ID_PRODUCTION}
STRIPE_ENTERPRISE_PRICE_ID=${STRIPE_ENTERPRISE_PRICE_ID_PRODUCTION}

# CORS Configuration
CORS_ORIGINS=${CORS_ORIGINS_PRODUCTION}

# Security Configuration
SECRET_KEY=${USER_SERVICE_SECRET_KEY_PRODUCTION}

# Production Mode Settings
LOG_LEVEL=info
RATE_LIMIT_ENABLED=true
METRICS_ENABLED=true
MAX_WORKERS=4
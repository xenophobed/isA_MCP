apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: isa-cloud-staging

resources:
  - mcp-deployment.yaml
  - mcp-service.yaml
  - mcp-configmap.yaml

commonLabels:
  app: mcp
  tier: applications
  project: isa-cloud
  managed-by: kustomize

commonAnnotations:
  description: "isA MCP Service - Model Context Protocol"
  version: "1.0.0"
